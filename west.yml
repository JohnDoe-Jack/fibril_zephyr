# Copyright (c) 2021 Nordic Semiconductor ASA
# SPDX-License-Identifier: Apache-2.0

manifest:
  self:
    west-commands: scripts/west-commands.yml

  remotes:
    - name: zephyrproject-rtos
      url-base: https://github.com/zephyrproject-rtos

  projects:
    - name: zephyr
      remote: zephyrproject-rtos
      revision: v4.4.1
      import:
        # By using name-allowlist we can clone only the modules that are
        # strictly needed by the application.
        name-allowlist:
          - cmsis_6 # required by the ARM port for Cortex-M
          - cmsis-dsp
          - hal_rpi_pico # RP2350 SoC support
          - hal_stm32 # required by the nucleo_f302r8 board (STM32 based)
          - segger # RTT console and logging
    - name: cannectivity
      url: https://github.com/CANnectivity/cannectivity.git
      revision: v1.4.0
      path: modules/third_party/cannectivity
    - name: fibril_can
      # Pinned, not tracking main: the codegen's block-array order is part of
      # the wire contract, and a change to it silently reassigns the counts an
      # application passes in fcan_config_t::instance_counts. Bump this
      # deliberately, together with any migration the release notes call for.
      #
      # Currently the merge commit of fibril_can PR #56 on main. #55 brought
      # the schema merging apps/node relies on (one node schema plus every
      # block type this repository implements), #56 the codegen lookup and the
      # generated-header visibility the build needs. Both are past v0.1.2, so
      # there is no tag to point at yet.
      revision: dedeacd8c35dee269be0c8da79e6e96af16d179a
      url: https://github.com/ForteFibre/fibril_can.git
      path: modules/lib/fibril_can
