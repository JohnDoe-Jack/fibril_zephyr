# PL011 in Zephyr v4.4.1 has interrupt-driven RX, not the async API.
CONFIG_ESP_NOW_GAMEPAD_BRIDGE_INTERRUPT=y
CONFIG_LOG_BACKEND_UART=y
CONFIG_LOG_BACKEND_RTT=n
CONFIG_RTT_CONSOLE=n
CONFIG_USE_SEGGER_RTT=n
